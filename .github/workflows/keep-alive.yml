name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the app to prevent sleep
        run: curl -s --max-time 30 "https://geo-audit-agent-mouk.onrender.com/api/health"

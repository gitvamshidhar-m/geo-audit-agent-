name: Keep FlareSolverr Alive

on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping FlareSolverr
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://flaresolverr-latest-z7w2.onrender.com || true
